<Window x:Class="FramelessWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="clr-namespace:FramelessWPF"
        Title="MainWindow" SnapsToDevicePixels="True" 
        BorderThickness="1" BorderBrush="#FF007ACC" 
        Height="590" Width="921" Style="{StaticResource MainWindowStyle}" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

        </Grid>

        <StatusBar Grid.Row="2" Height="23" Background="#FF007ACC"/>

    </Grid>
</Window>
